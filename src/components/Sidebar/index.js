import React from 'react';
import './style.css';
import Card from '../../components/UI/Card';

const Sidebar = (props) => {
	return (
		<Card style={{width: '27%'}}>
			My first blog post
		</Card>
	);
}

export default Sidebar;